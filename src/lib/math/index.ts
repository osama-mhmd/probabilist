import type { StatisticalDistribution } from '@/types'

function getbionomialDistribution(data: StatisticalDistribution): number {
  // TODO: Implement bionomial distribution calculation
  console.log(data)

  return 0
}

function getVariance(n: number, p: number | number[]): number {
  // TODO: Implement variance calculation
  console.log(n, p)

  return 0
}

export { getbionomialDistribution, getVariance }
